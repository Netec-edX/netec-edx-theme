<%! from django.utils.translation import ugettext as _ %>
${_("Gracias por inscribirte a {platform_name}.").format(platform_name=settings.PLATFORM_NAME)}

${_("Hace poco tiempo solicitaste una suscripción a "
    "{platform_name}. Haz click en el siguiente link para validar tu cuenta"
    ).format(
      platform_name=settings.PLATFORM_NAME
    )}

% if is_secure:
  https://edx.netecdigital.com/activate/${ key }
% else:
  http://edx.netecdigital.com/activate/${ key }
% endif

${_("Si no la solicitaste, no necesitas hacer nada."
      "Por favor, no respondas este correo. Si necesitas ayuda, contáctanos en"
      "informes@netecdigital.com"
      .format(platform_name=settings.PLATFORM_NAME)}
